// IMPORTED FROM THE SAME FOLDER.
import AppLayout from './AppLayout.tsx';

function AppLayoutSpinner() {
	return (
		<AppLayout>
			<div className='appLayoutSpinner'>LoadingSpinner</div>
		</AppLayout>
	);
}

export default AppLayoutSpinner;
